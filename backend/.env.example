DATABASE_URL=sqlite:///./app.db
CORS_ORIGINS=["http://localhost:5173"]

# Required for the improvement agent (Claude with web_fetch).
ANTHROPIC_API_KEY=

# Optional Peec wiring (only needed for real MCP-driven action generation;
# fixtures cover the demo path otherwise).
PEEC_API_KEY=
PEEC_USE_REAL_MCP=false
